backups/
work/NotePlanURLDrop/NotePlanURLDrop
.DS_Store

# Build artifacts regenerated by scripts/build-*.sh — already tracked copies stay
# tracked until explicitly removed from git; this only stops new ones piling up.
outputs/*.app/

# Ad-hoc test/screenshot files dropped in work/ during manual QA sessions.
work/*.png
